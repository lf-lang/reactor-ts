//@flow
import { Gain } from "./gain";

describe('gain', () => {
    // it('1 after gain 2 should equal to 2', () => {
    //     let gain = new Gain("My Gain");
    //     gain.setup();
    //     gain.initialize();
    //     gain.provideInput("input", 1);
    //     gain.setParameter('gain', 2);

    //     let output_before_fire = gain.get("scaled");
    //     expect(output_before_fire).toBe(undefined);

    //     gain.fire();

    //     let output = gain.get("scaled");
    //     expect(output).toBe(2);
    // });
});
